function getExpirience(){
  const year = document.getElementById('num');
  const random = Math.floor(Math.random (2022)-(year));
  const button = document.getElementById('but').innerHTML('onclick')
  return button
}
