// function calculateAmountOfNull(str) {
//   let a = 0
//   for (let i = 0; i < 0; i++) {
//     if(array[i]===0) counter++
//   }
//   return counter
// }
//
// document.write(calculateAmountOfNull(input))

let el = [1, -2, 2, 3, -4];
let sum = arr.reduce((newRes, el) => newRes(el>0) + newRes);

document.write(`${sum}`)
