// let num = Math.floor(Math.random()*19+1);

  document.write('<table border="2px">');
    document.write("<tr>")
      const lastNum = Math.floor(Math.random()*22-1);

    for (var i = 0; i < lastNum; i++) {
      document.write(`<td>${lastNum}</td>
    `);
    }
    document.write("</tr>")
